import React from 'react';
import { Button } from 'antd';

const Lazy = () => {
  return (
    <div>
      <h1>Lazy component</h1>
      <Button type="primary">button</Button>
    </div>
  );
};

export default Lazy;